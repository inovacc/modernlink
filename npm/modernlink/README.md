# @inovacc/modernlink

This package is a distribution launcher only. It resolves an exact platform package and starts
the ModernLink Rust binary; it contains no repository-analysis logic.

It is intentionally private until release artifacts and GitHub Packages publication are
configured. The release workflow publishes the `@inovacc` scope to
`https://npm.pkg.github.com`, not to npmjs.org.
